# URL of the deployed backend (no trailing slash). Set this in Vercel's
# project settings; for local dev, frontend/.env.local can leave it unset
# (falls back to http://localhost:8000).
VITE_API_URL=https://your-username-hemosmart.hf.space
